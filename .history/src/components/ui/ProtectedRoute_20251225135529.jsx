
import React from 'react'

const ProtectedRoute = (c{children}) => {
    const {isSignedIn, user, isLoaded}=userUser()
    const {Pathname}=useLocation()
  return (
    <div>
      
    </div>
  )
}

export default ProtectedRoute
